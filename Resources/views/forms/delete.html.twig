{% extends "@c975LGiftVoucher/layout.html.twig" %}
{% trans_default_domain 'giftVoucher' %}
{% set title = 'label.delete'|trans({}, 'toolbar') ~ ' "' ~ giftVoucher.object ~ '"' %}

{% block giftVoucher_content %}
{# Toolbar #}
    {{ toolbar|raw }}

{# Form #}
    {{ form_start(form) }}
        {{ form_widget(form) }}
{# Submit #}
        <div class="form-group">
            <button type="submit" name="submit" title="{{ 'label.delete'|trans({}, 'toolbar') }}" class="btn btn-lg btn-danger btn-block">
                <span class="glyphicon glyphicon-trash"></span> {{ 'label.delete'|trans({}, 'toolbar') }}
            </button>
        </div>
    {{ form_end(form) }}
{% endblock %}